'use strict'
const path = require('path');

module.exports = {
	dev: {
    assetsPublicPath: '/',

    // dev server
    port: 8080,
    proxyTable: {
      // '/': 'http://localhost:9080',
    },
	},

	build: {
	},
};
